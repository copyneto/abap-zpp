@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_PP_RECEITA_MESTRE {
    grant
        select
            on
                ZC_PP_RECEITA_MESTRE
                    where
                        inheriting conditions from entity ZI_PP_RECEITA_MESTRE;
                        
}